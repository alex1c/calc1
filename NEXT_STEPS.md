# 🚀 Следующие шаги после настройки секретов

## ✅ Шаг 1: Закоммитьте и запушьте изменения

На вашем локальном компьютере:

```bash
git add .github/workflows/deploy.yml .github/SETUP_SECRETS.md
git commit -m "Add deployment workflow with secrets validation"
git push origin main
```

## 🔧 Шаг 2: Подготовьте сервер

Убедитесь, что на сервере все готово:

### 2.1. Проверьте наличие Docker и Docker Compose

```bash
# На сервере
docker --version
docker compose version || docker-compose --version
```

Если не установлены, установите:

```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

### 2.2. Убедитесь, что репозиторий клонирован

```bash
cd /var/www/calc1.ru
git remote -v
# Должно показать: origin  https://github.com/alex1c/calc1.git
```

Если репозиторий не клонирован:

```bash
sudo mkdir -p /var/www/calc1.ru
sudo chown $USER:$USER /var/www/calc1.ru
cd /var/www/calc1.ru
git clone https://github.com/alex1c/calc1.git .
```

### 2.3. Проверьте права доступа

```bash
chmod +x scripts/*.sh
sudo chown -R $USER:$USER /var/www/calc1.ru
```

## 🧪 Шаг 3: Протестируйте деплой

### Вариант A: Автоматический тест через GitHub Actions

После push в `main`, GitHub Actions автоматически запустит деплой:

1. Перейдите в **Actions** в репозитории GitHub
2. Выберите последний workflow run
3. Проверьте, что все шаги выполнены успешно

### Вариант B: Ручной запуск через GitHub UI

1. Перейдите в **Actions** → **Deploy to Production**
2. Нажмите **Run workflow**
3. Выберите ветку `main`
4. Нажмите **Run workflow**

### Вариант C: Ручной деплой на сервере (для тестирования)

```bash
# На сервере
cd /var/www/calc1.ru
git pull origin main
chmod +x scripts/*.sh
./scripts/deploy.sh
```

## 🔍 Шаг 4: Проверьте работу приложения

После успешного деплоя:

```bash
# На сервере проверьте статус контейнера
docker compose ps || docker-compose ps

# Проверьте логи
docker compose logs -f || docker-compose logs -f

# Проверьте работу приложения
curl http://localhost:3000
```

## 🌐 Шаг 5: Настройте Apache (если еще не настроен)

```bash
# Включите необходимые модули
sudo a2enmod proxy proxy_http rewrite headers

# Скопируйте конфигурацию
sudo cp /var/www/calc1.ru/apache/calc1.ru.conf /etc/apache2/sites-available/calc1.ru.conf

# Включите сайт
sudo a2ensite calc1.ru.conf

# Перезагрузите Apache
sudo systemctl reload apache2
```

## 🔒 Шаг 6: Настройте SSL (рекомендуется)

```bash
# Установите Certbot
sudo apt install certbot python3-certbot-apache

# Получите сертификат
sudo certbot --apache -d calc1.ru -d www.calc1.ru
```

## 📊 Мониторинг деплоев

После настройки, каждый push в ветку `main` будет автоматически деплоить приложение.

Проверить статус деплоев можно:
- В GitHub: **Actions** → **Deploy to Production**
- На сервере: `docker compose ps` или `docker-compose ps`

## 🆘 Если что-то пошло не так

### Проблема: GitHub Actions не может подключиться

1. Проверьте, что секреты правильно настроены
2. Проверьте SSH ключ: `cat ~/.ssh/github_actions.pub` должен быть в `~/.ssh/authorized_keys`
3. Проверьте подключение вручную: `ssh -i ~/.ssh/github_actions user@server-ip`

### Проблема: Docker не запускается

```bash
# Проверьте логи
docker compose logs || docker-compose logs

# Пересоберите контейнер
docker compose down -v
docker compose build --no-cache
docker compose up -d
```

### Проблема: Приложение не отвечает

```bash
# Проверьте, что контейнер запущен
docker compose ps

# Проверьте порт
sudo netstat -tulpn | grep 3000

# Проверьте логи
docker compose logs -f
```

## ✅ Чеклист готовности

- [ ] Секреты настроены в GitHub
- [ ] Изменения закоммичены и запушены
- [ ] Docker установлен на сервере
- [ ] Репозиторий клонирован на сервере
- [ ] Права доступа настроены
- [ ] Первый деплой выполнен успешно
- [ ] Приложение доступно на localhost:3000
- [ ] Apache настроен как reverse proxy (опционально)
- [ ] SSL сертификат установлен (опционально)

---

**После выполнения всех шагов, ваш сайт будет автоматически обновляться при каждом push в ветку `main`!** 🎉

