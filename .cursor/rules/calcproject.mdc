---
alwaysApply: true
---
Мы делаем сайт со множеством калькуляторов. При создании нового калькулятора используй единообразие с уже созданными.
Используй единообразие в структуре проекта
Все калькуляторы переведены на несколько языков. Используй единый подход для всего сайта для перевода.
На страницу каждого калькулятора добавляй максимальную сео информацию. Сео блоки, заголовки, разметку. Максимум ключевых слов
При добавлении СЕО всегда учитывай домен на котором будет расположен сайт calc1.ru
При наличии списков не забывай переводить значения выпадающих списков на соответствующие локали
всегда добавляй SEO-блоки


<title> — с переводом.

<meta name="description">

Сниппет для поисковиков с FAQ structured data (JSON-LD):
Убедитесь, что разметка соответствует стандартам Schema.org и правилам поисковых систем. 



## 📋 Правила единообразия переводов для проекта 

### 1. **Структурные правила**

#### 1.1 Единая структура переводов
```json
{
  "common": { /* общие элементы */ },
  "brand": { /* брендинг */ },
  "breadcrumbs": { /* навигация */ },
  "navigation": { /* меню */ },
  "categories": { /* категории */ },
  "calculators": { /* ВСЕ калькуляторы */ }
}
```

#### 1.2 Обязательная структура для каждого калькулятора
```json
{
  "calculators": {
    "calculator-name": {
      "title": "Заголовок",
      "description": "Описание",
      "breadcrumbs": { /* хлебные крошки */ },
      "form": {
        "title": "Заголовок формы",
        "fieldName": "Название поля",
        "dropdownName": { /* значения выпадающих списков */ },
        "errors": { /* ошибки валидации */ }
      },
      "results": {
        "title": "Заголовок результатов",
        "fieldName": "Название поля результата"
      },
      "seo": {
        "title": "SEO заголовок",
        "overview": { /* описание */ },
        "calculation": { /* как рассчитывается */ },
        "advantages": { /* преимущества */ },
        "tips": { /* советы */ }
      }
    }
  }
}
```

### 2. **Правила именования ключей**

#### 2.1 Формат ключей
- ✅ **Правильно:** `camelCase` - `carValue`, `engineVolume`, `fuelTypes`
- ❌ **Неправильно:** `snake_case`, `kebab-case`, `PascalCase`

#### 2.2 Обязательные суффиксы
- **Выпадающие списки:** `Types` - `fuelTypes`, `carTypes`, `paymentTypes`
- **Ошибки:** `errors` - `form.errors.validation`
- **Результаты:** `results` - `results.monthlyPayment`

### 3. **Правила для выпадающих списков**

#### 3.1 Обязательная структура
```json
{
  "form": {
    "fuelTypes": {
      "gasoline": "Бензин",
      "diesel": "Дизель",
      "electric": "Электрический"
    }
  }
}
```

#### 3.2 Использование в компонентах
```javascript
// ❌ НЕПРАВИЛЬНО - захардкоженные значения
const options = [
  { value: 'gasoline', label: 'Бензин' }
];

// ✅ ПРАВИЛЬНО - через переводы
const options = [
  { value: 'gasoline', label: t('form.fuelTypes.gasoline') }
];
```

### 4. **Правила валют и единиц измерения**

#### 4.1 Локализованные валюты
- **Русский (ru):** ₽ (рубли), л.с. (лошадиные силы)
- **Английский (en):** $ (доллары), hp (horsepower)
- **Испанский (es):** € (евро), hp (horsepower)
- **Немецкий (de):** € (евро), PS (Pferdestärken)

#### 4.2 Формат в переводах
```json
{
  "carValue": "Стоимость автомобиля (₽)",
  "enginePower": "Мощность двигателя (л.с.)"
}
```

### 5. **Правила проверки переводов**

#### 5.1 Обязательные проверки перед коммитом
1. **Все ключи присутствуют** во всех языках
2. **Выпадающие списки переведены** на все языки
3. **Валюты локализованы** правильно
4. **Нет русских текстов** в английских/испанских/немецких файлах

#### 5.2 Скрипт автоматической проверки
```javascript
// scripts/check-translations.js
const requiredKeys = [
  'title', 'description', 'breadcrumbs', 'form', 'results', 'seo'
];

const requiredFormKeys = [
  'title', 'errors'
];

const requiredDropdownKeys = [
  // Список всех выпадающих списков
];
```

### 6. **Правила для page.tsx файлов**

#### 6.1 Единообразное использование useTranslations
```javascript
// ✅ ПРАВИЛЬНО
const t = useTranslations('calculators.calculator-name');

// ❌ НЕПРАВИЛЬНО - смешанные пути
const t = useTranslations('calculator-name');
const t = useTranslations('calculators.calculator-name');
```

#### 6.2 Единообразное использование generateMetadata
```javascript
// ✅ ПРАВИЛЬНО
const t = (key: string) => messages.calculators['calculator-name'].seo[key];

// ❌ НЕПРАВИЛЬНО - разные пути
const t = (key: string) => messages.calculator-name.seo[key];
```

### 7. **Правила для компонентов калькуляторов**

#### 7.1 Обязательные импорты
```javascript
import { useTranslations } from 'next-intl';

export default function CalculatorComponent() {
  const t = useTranslations('calculators.calculator-name');
  // ...
}
```

#### 7.2 Обязательная структура переводов в компонентах
```javascript
// Все тексты через переводы
<h1>{t('title')}</h1>
<p>{t('description')}</p>

// Выпадающие списки через переводы
<select>
  {Object.entries(t.raw('form.dropdownTypes')).map(([key, value]) => (
    <option key={key} value={key}>{value}</option>
  ))}
</select>
```

### 8. **Правила для новых калькуляторов**

#### 8.1 Обязательный чеклист
- [ ] Создать секцию в `calculators.calculator-name`
- [ ] Добавить все ключи во все языки (ru, en, es, de)
- [ ] Перевести выпадающие списки
- [ ] Локализовать валюты и единицы измерения
- [ ] Проверить breadcrumbs
- [ ] Добавить SEO контент
- [ ] Протестировать на всех языках

#### 8.2 Шаблон нового калькулятора
```json
{
  "calculators": {
    "new-calculator": {
      "title": "Название калькулятора",
      "description": "Описание калькулятора",
      "breadcrumbs": {
        "category": "Категория"
      },
      "form": {
        "title": "Параметры расчёта",
        "field1": "Поле 1 (₽)",
        "field2": "Поле 2 (л.с.)",
        "dropdownTypes": {
          "option1": "Опция 1",
          "option2": "Опция 2"
        },
        "errors": {
          "title": "Ошибки в данных"
        }
      },
      "results": {
        "title": "Результаты расчёта",
        "result1": "Результат 1",
        "result2": "Результат 2"
      },
      "seo": {
        "title": "SEO заголовок",
        "overview": {
          "title": "О калькуляторе",
          "content": "Описание калькулятора"
        },
        "calculation": {
          "title": "Как рассчитывается",
          "content": "Описание расчёта"
        },
        "advantages": {
          "title": "Преимущества",
          "content": "Преимущества калькулятора"
        },
        "tips": {
          "title": "Советы",
          "content": "Полезные советы"
        }
      }
    }
  }
}
```

### 9. **Автоматизация и инструменты**

#### 9.1 Скрипты для проверки
```bash
# Проверка структуры переводов
npm run check-translations

# Проверка выпадающих списков
npm run check-dropdowns

# Проверка валют
npm run check-currencies

# Полная проверка
npm run check-all-translations
```

#### 9.2 Pre-commit хуки
```json
{
  "husky": {
    "hooks": {
      "pre-commit": "npm run check-all-translations"
    }
  }
}
```

### 10. **Правила для команды разработки**

#### 10.1 Обязательные действия
1. **Перед добавлением нового калькулятора** - проверить существующие переводы
2. **При изменении существующего калькулятора** - обновить все языки
3. **При добавлении выпадающего списка** - добавить переводы во все языки
4. **Перед коммитом** - запустить проверку переводов

#### 10.2 Запрещенные действия
- ❌ Добавлять русские тексты в английские/испанские/немецкие файлы
- ❌ Захардкоживать значения выпадающих списков
- ❌ Использовать разные пути для useTranslations
- ❌ Пропускать переводы для новых ключей

### 11. **Мониторинг и поддержка**

#### 11.1 Регулярные проверки
- **Еженедельно:** Проверка всех калькуляторов на всех языках
- **При каждом релизе:** Полная проверка переводов
- **При добавлении нового языка:** Адаптация всех правил

#### 11.2 Документация
- Ведение списка всех калькуляторов и их ключей
- Документирование новых правил
- Обновление шаблонов при изменении структуры

Эти правила обеспечат единообразие переводов и предотвратят появление ошибок в будущем. Главное - следовать им неукоснительно и автоматизировать проверки.