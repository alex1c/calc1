# 📋 Структура проекта Calc1.ru

## 🎯 Общее описание проекта

**Calc1.ru** — это многоязычный веб-сайт с коллекцией онлайн-калькуляторов различных категорий. Проект построен на Next.js 14 с использованием App Router, TypeScript, и поддерживает 9 языков интерфейса.

### Основные характеристики:
- **Технологии**: Next.js 14, React 18, TypeScript, Tailwind CSS
- **Интернационализация**: 9 языков (ru, en, de, es, fr, it, pl, tr, pt-BR)
- **Архитектура**: Модульная структура с разделением логики расчетов и UI
- **SEO**: Полная поддержка метаданных, структурированных данных Schema.org
- **Тестирование**: Jest для unit-тестов, Playwright для E2E

---

## 📁 Структура директорий проекта

```
calc1/
├── src/                          # Исходный код приложения
│   ├── app/                      # Next.js App Router
│   │   ├── [locale]/            # Локализованные страницы
│   │   │   ├── auto/             # Калькуляторы категории "Авто"
│   │   │   ├── construction/    # Калькуляторы категории "Строительство"
│   │   │   ├── finance/         # Калькуляторы категории "Финансы"
│   │   │   ├── life/             # Калькуляторы категории "Жизнь"
│   │   │   ├── math/             # Калькуляторы категории "Математика"
│   │   │   ├── time/             # Калькуляторы категории "Время"
│   │   │   ├── health/          # Калькуляторы категории "Здоровье"
│   │   │   ├── converter/       # Калькуляторы категории "Конвертеры"
│   │   │   ├── fun/              # Калькуляторы категории "Развлечения"
│   │   │   ├── it/               # Калькуляторы категории "IT"
│   │   │   ├── science/          # Калькуляторы категории "Наука"
│   │   │   ├── page.tsx          # Главная страница
│   │   │   └── layout.tsx        # Лейаут для локализованных страниц
│   │   ├── api/                  # API маршруты
│   │   │   ├── bmi/              # API для BMI калькулятора
│   │   │   ├── calories/         # API для калькулятора калорий
│   │   │   ├── contact/          # API для контактной формы
│   │   │   ├── converter/        # API для конвертера единиц
│   │   │   ├── equations/        # API для решения уравнений
│   │   │   ├── food-ration/      # API для расчета рациона
│   │   │   ├── pregnancy/        # API для калькулятора беременности
│   │   │   ├── search-index/     # API для поискового индекса
│   │   │   └── statistics/       # API для статистики
│   │   ├── globals.css           # Глобальные стили
│   │   ├── layout.tsx            # Корневой лейаут
│   │   ├── robots.ts             # Robots.txt генератор
│   │   └── sitemap.ts            # Sitemap генератор
│   ├── components/               # React компоненты
│   │   ├── calculators/          # Компоненты калькуляторов (111 файлов)
│   │   │   ├── basic-math-calculator.tsx
│   │   │   ├── bmi-calculator.tsx
│   │   │   ├── concrete-calculator.tsx
│   │   │   ├── floor-heating-calculator.tsx
│   │   │   └── ...               # Остальные калькуляторы
│   │   ├── seo/                  # SEO компоненты (110 файлов)
│   │   │   ├── basic-math-seo.tsx
│   │   │   ├── bmi-seo.tsx
│   │   │   └── ...               # SEO контент для каждого калькулятора
│   │   ├── breadcrumbs.tsx       # Компонент хлебных крошек
│   │   ├── category-card.tsx     # Карточка категории
│   │   ├── contact-form.tsx      # Форма обратной связи
│   │   ├── footer.tsx            # Футер сайта
│   │   ├── header.tsx            # Хедер сайта
│   │   ├── header-wrapper.tsx    # Обертка хедера
│   │   ├── search-modal.tsx      # Модальное окно поиска
│   │   └── seo-content.tsx      # Общий SEO компонент
│   ├── lib/                      # Библиотеки и утилиты
│   │   ├── calculators/          # Логика расчетов (80 файлов)
│   │   │   ├── __tests__/        # Unit-тесты для калькуляторов
│   │   │   │   ├── area.test.ts
│   │   │   │   ├── bmi.test.ts
│   │   │   │   ├── loan.test.ts
│   │   │   │   ├── percent.test.ts
│   │   │   │   └── volume.test.ts
│   │   │   ├── add-time.ts       # Расчет добавления времени
│   │   │   ├── age.ts            # Расчет возраста
│   │   │   ├── alcohol.ts        # Расчет алкоголя в крови
│   │   │   ├── area.ts           # Расчет площади
│   │   │   ├── basic-math.ts     # Базовые математические операции
│   │   │   ├── bmi.ts            # Расчет индекса массы тела
│   │   │   ├── calories.ts       # Расчет калорий
│   │   │   ├── concrete.ts       # Расчет бетона
│   │   │   ├── equations.ts      # Решение уравнений
│   │   │   ├── floor-heating.ts  # Расчет теплого пола
│   │   │   ├── loan.ts           # Расчет кредита
│   │   │   ├── percent.ts        # Расчет процентов
│   │   │   ├── volume.ts         # Расчет объема
│   │   │   └── ...               # Остальные калькуляторы
│   │   ├── i18n-utils.ts         # Утилиты для загрузки переводов
│   │   ├── seo-utils.ts          # Утилиты для SEO
│   │   └── search-utils.ts       # Утилиты для поиска
│   ├── hooks/                    # React хуки
│   │   └── use-search-index.ts   # Хук для поискового индекса
│   ├── data/                     # Статические данные
│   │   └── ...                   # JSON файлы с данными
│   ├── i18n.ts                   # Конфигурация интернационализации
│   └── middleware.ts             # Middleware для обработки локалей
├── messages/                     # Файлы переводов
│   ├── ru/                       # Русский язык
│   │   ├── calculators/          # Переводы калькуляторов по категориям
│   │   │   ├── auto.json         # Калькуляторы категории "Авто"
│   │   │   ├── construction.json # Калькуляторы категории "Строительство"
│   │   │   ├── finance.json      # Калькуляторы категории "Финансы"
│   │   │   ├── life.json         # Калькуляторы категории "Жизнь"
│   │   │   ├── math.json         # Калькуляторы категории "Математика"
│   │   │   ├── time.json         # Калькуляторы категории "Время"
│   │   │   ├── health.json       # Калькуляторы категории "Здоровье"
│   │   │   ├── converter.json    # Калькуляторы категории "Конвертеры"
│   │   │   ├── fun.json          # Калькуляторы категории "Развлечения"
│   │   │   └── it.json           # Калькуляторы категории "IT"
│   │   └── index.json            # Индекс переводов (если используется)
│   ├── en/                       # Английский язык (та же структура)
│   ├── de/                       # Немецкий язык
│   ├── es/                       # Испанский язык
│   ├── fr/                       # Французский язык
│   ├── it/                       # Итальянский язык
│   ├── pl/                       # Польский язык
│   ├── tr/                       # Турецкий язык
│   ├── pt-BR/                    # Португальский (Бразилия)
│   ├── ru.json                   # Монолитный файл переводов (обратная совместимость)
│   ├── en.json                   # Монолитный файл переводов
│   └── ...                       # Монолитные файлы для остальных языков
├── scripts/                      # Служебные скрипты
│   ├── check-translations.js     # Проверка синхронизации переводов
│   ├── sync-translations.js      # Синхронизация структуры переводов
│   ├── clean-monolith-files.js   # Очистка монолитных файлов
│   └── run-tests.js              # Запуск тестов
├── e2e/                          # E2E тесты (Playwright)
│   ├── calculators.spec.ts       # Тесты калькуляторов
│   ├── homepage.spec.ts          # Тесты главной страницы
│   └── language.spec.ts          # Тесты локализации
├── docs/                         # Документация
│   ├── FAVICONS_SETUP.md         # Настройка favicon
│   ├── pdf-export-component.md   # Компонент экспорта PDF
│   ├── SEO_IMPLEMENTATION.md     # Реализация SEO
│   └── PROJECT_STRUCTURE.md      # Этот файл
├── public/                       # Статические файлы
│   └── manifest.json             # Web App Manifest
├── next.config.js                # Конфигурация Next.js
├── tailwind.config.js            # Конфигурация Tailwind CSS
├── tsconfig.json                 # Конфигурация TypeScript
├── jest.config.js                # Конфигурация Jest
├── playwright.config.ts          # Конфигурация Playwright
└── package.json                  # Зависимости проекта
```

---

## 🗂️ Структура хранения данных

### 1. Переводы (Internationalization)

Проект использует **гибридную систему хранения переводов** для обеспечения обратной совместимости и удобства разработки.

#### Структура файлов переводов:

**Вариант 1: Монолитные файлы (legacy, обратная совместимость)**
```
messages/
├── ru.json          # Все переводы в одном файле
├── en.json          # Все переводы в одном файле
└── ...
```

**Вариант 2: Модульные файлы по категориям (рекомендуемый)**
```
messages/
├── ru/
│   ├── calculators/
│   │   ├── auto.json           # Калькуляторы категории "Авто"
│   │   ├── construction.json   # Калькуляторы категории "Строительство"
│   │   ├── finance.json        # Калькуляторы категории "Финансы"
│   │   └── ...
│   └── index.json              # Общие переводы (если используется)
└── en/
    └── calculators/
        └── ...                 # Та же структура
```

#### Структура JSON файла перевода калькулятора:

```json
{
  "calculators": {
    "calculator-name": {
      "title": "Название калькулятора",
      "description": "Описание калькулятора",
      "breadcrumbs": {
        "category": "Название категории"
      },
      "form": {
        "title": "Заголовок формы",
        "fieldName": "Название поля",
        "dropdownName": {
          "option1": "Опция 1",
          "option2": "Опция 2"
        },
        "errors": {
          "title": "Ошибки в данных",
          "fieldName": "Сообщение об ошибке"
        }
      },
      "results": {
        "title": "Заголовок результатов",
        "fieldName": "Название поля результата"
      },
      "options": {
        "dropdownTypes": {
          "option1": "Опция 1",
          "option2": "Опция 2"
        }
      },
      "hero": {
        "format": "Единицы измерения",
        "accuracy": "Точность расчётов"
      },
      "features": {
        "feature1": "Описание функции 1",
        "feature2": "Описание функции 2"
      },
      "seo": {
        "title": "SEO заголовок",
        "description": "SEO описание",
        "overview": {
          "title": "О калькуляторе",
          "content": "Описание калькулятора",
          "additionalContent": "Дополнительное описание"
        },
        "calculation": {
          "title": "Как рассчитывается",
          "content": "Описание метода расчета"
        },
        "advantages": {
          "title": "Преимущества",
          "content": "Описание преимуществ"
        },
        "tips": {
          "title": "Советы",
          "content": "Полезные советы"
        },
        "faq": {
          "title": "Часто задаваемые вопросы",
          "faqItems": [
            {
              "q": "Вопрос",
              "a": "Ответ"
            }
          ]
        },
        "howTo": {
          "title": "Как использовать",
          "description": "Описание процесса",
          "step1": {
            "name": "Шаг 1",
            "text": "Описание шага 1"
          },
          "step2": {
            "name": "Шаг 2",
            "text": "Описание шага 2"
          }
        }
      }
    }
  }
}
```

#### Правила именования ключей:

1. **Формат ключей**: Используется `kebab-case` для имен калькуляторов
   - ✅ Правильно: `calculators.floor-heating.title`
   - ❌ Неправильно: `calculators.floorHeating.title` или `calculators.floor_heating.title`

2. **Структура секций**:
   - `calculators.{calculator-name}.title` - заголовок
   - `calculators.{calculator-name}.description` - описание
   - `calculators.{calculator-name}.form.*` - поля формы
   - `calculators.{calculator-name}.results.*` - результаты
   - `calculators.{calculator-name}.seo.*` - SEO контент

3. **Выпадающие списки**: Всегда переводятся
   - Хранятся в `form.{dropdownName}` или `options.{dropdownTypes}`
   - Используются через `t.raw('form.dropdownName')`

4. **Валюты и единицы измерения**: Локализуются
   - Русский: ₽ (рубли), л.с. (лошадиные силы)
   - Английский: $ (доллары), hp (horsepower)
   - Испанский: € (евро), hp (horsepower)
   - Немецкий: € (евро), PS (Pferdestärken)

#### Загрузка переводов:

**В компонентах (Client Components):**
```typescript
import { useTranslations } from 'next-intl';

export default function CalculatorComponent() {
  const t = useTranslations('calculators.calculator-name');
  
  return <h1>{t('title')}</h1>;
}
```

**В страницах (Server Components):**
```typescript
import { getTranslations } from 'next-intl/server';

export default async function Page({ params: { locale } }) {
  const t = await getTranslations({
    locale,
    namespace: 'calculators.calculator-name',
  });
  
  return <h1>{t('title')}</h1>;
}
```

**В generateMetadata:**
```typescript
export async function generateMetadata({ params: { locale } }) {
  const { loadMergedCategoryTranslations } = await import('@/lib/i18n-utils');
  const messages = await loadMergedCategoryTranslations(locale, 'category');
  const t = (key: string) => messages.calculators['calculator-name'].seo[key];
  
  return {
    title: t('title'),
    description: t('description'),
  };
}
```

#### Процесс объединения переводов:

1. **Базовый монолитный файл** (`messages/{locale}.json`) загружается первым
2. **Категорийные файлы** (`messages/{locale}/calculators/{category}.json`) загружаются и объединяются через `deepMerge`
3. **Результат**: Единый объект `messages` с объединенными переводами

**Функция deepMerge:**
- Массивы заменяются полностью
- Объекты объединяются рекурсивно
- Приоритет у категорийных файлов над монолитными

---

### 2. Логика расчетов (Calculation Logic)

Логика расчетов хранится в отдельных TypeScript файлах в директории `src/lib/calculators/`.

#### Структура файла калькулятора:

```typescript
/**
 * Calculator Name Library
 * 
 * Provides functionality for [description].
 * 
 * Features:
 * - Feature 1
 * - Feature 2
 * 
 * Calculation method:
 * - Formula description
 */

// Input interface
export interface CalculatorInput {
  field1: number;
  field2: string;
  // ...
}

// Result interface
export interface CalculatorResult {
  result1: number;
  result2: string;
  // ...
}

// Validation function
export function validateCalculatorInput(
  input: CalculatorInput
): { isValid: boolean; errors: string[] } {
  const errors: string[] = [];
  
  if (!input.field1 || input.field1 <= 0) {
    errors.push('Field1 must be greater than 0');
  }
  
  return { isValid: errors.length === 0, errors };
}

// Main calculation function
export function calculateCalculator(
  input: CalculatorInput
): CalculatorResult {
  const validation = validateCalculatorInput(input);
  if (!validation.isValid) {
    throw new Error(validation.errors.join(', '));
  }
  
  // Calculation logic
  const result1 = input.field1 * 2;
  const result2 = `Result: ${result1}`;
  
  return {
    result1,
    result2,
  };
}

// Helper functions
export function formatResult(value: number): string {
  return `${value.toFixed(2)}`;
}
```

#### Принципы организации логики:

1. **Разделение ответственности**:
   - Логика расчетов → `src/lib/calculators/{calculator-name}.ts`
   - UI компоненты → `src/components/calculators/{calculator-name}-calculator.tsx`
   - Страницы → `src/app/[locale]/{category}/{calculator-name}/page.tsx`

2. **Типизация**:
   - Все функции типизированы через TypeScript interfaces
   - Входные данные: `{CalculatorName}Input`
   - Результаты: `{CalculatorName}Result`

3. **Валидация**:
   - Каждый калькулятор имеет функцию `validate{CalculatorName}Input`
   - Возвращает `{ isValid: boolean; errors: string[] }`

4. **Чистые функции**:
   - Функции расчетов не имеют побочных эффектов
   - Легко тестируются
   - Могут использоваться на клиенте и сервере

5. **Документация**:
   - Все функции документированы через JSDoc комментарии
   - Описаны параметры, возвращаемые значения, алгоритмы

#### Пример использования в компоненте:

```typescript
'use client';

import { useState } from 'react';
import { useTranslations } from 'next-intl';
import {
  calculateCalculator,
  validateCalculatorInput,
  type CalculatorInput,
  type CalculatorResult,
} from '@/lib/calculators/calculator-name';

export default function CalculatorComponent() {
  const t = useTranslations('calculators.calculator-name');
  
  const [input, setInput] = useState<Partial<CalculatorInput>>({
    field1: 0,
    field2: '',
  });
  const [result, setResult] = useState<CalculatorResult | null>(null);
  const [errors, setErrors] = useState<string[]>([]);
  
  const handleCalculate = () => {
    const validation = validateCalculatorInput(input as CalculatorInput);
    if (!validation.isValid) {
      setErrors(validation.errors);
      return;
    }
    
    try {
      const calculated = calculateCalculator(input as CalculatorInput);
      setResult(calculated);
      setErrors([]);
    } catch (error) {
      setErrors([(error as Error).message]);
    }
  };
  
  return (
    // JSX компонента
  );
}
```

---

### 3. Структура страниц калькуляторов

Каждый калькулятор имеет следующую структуру:

```
src/app/[locale]/{category}/{calculator-name}/
└── page.tsx                    # Страница калькулятора
```

#### Структура page.tsx:

```typescript
import { getTranslations } from 'next-intl/server';
import { Metadata } from 'next';
import { notFound } from 'next/navigation';
import Header from '@/components/header';
import CalculatorComponent from '@/components/calculators/{calculator-name}-calculator';
import CalculatorSEO from '@/components/seo/{calculator-name}-seo';
import Breadcrumbs from '@/components/breadcrumbs';
import SoftwareApplicationSchema from '@/components/seo/software-application-schema';

interface Props {
  params: { locale: string };
}

// Генерация метаданных
export async function generateMetadata({
  params: { locale },
}: Props): Promise<Metadata> {
  // Валидация локали
  if (!['ru', 'en', 'de', 'es', 'fr', 'it', 'pl', 'tr', 'pt-BR'].includes(locale)) {
    notFound();
  }
  
  // Загрузка переводов для метаданных
  const { loadMergedCategoryTranslations } = await import('@/lib/i18n-utils');
  const messages = await loadMergedCategoryTranslations(locale, 'category');
  const t = (key: string) => messages.calculators['calculator-name'].seo[key];
  
  return {
    title: `${t('title')} | Calc1.ru`,
    description: t('description'),
    keywords: [...],
    // ... остальные метаданные
  };
}

// Основной компонент страницы
export default async function CalculatorPage({ params: { locale } }: Props) {
  // Валидация локали
  if (!['ru', 'en', 'de', 'es', 'fr', 'it', 'pl', 'tr', 'pt-BR'].includes(locale)) {
    notFound();
  }
  
  // Загрузка переводов
  const t = await getTranslations({
    locale,
    namespace: 'calculators.calculator-name',
  });
  
  const tCategories = await getTranslations({
    locale,
    namespace: 'categories',
  });
  
  // Хлебные крошки
  const breadcrumbItems = [
    {
      label: tCategories('category.title'),
      href: '/category',
    },
    {
      label: t('title'),
    },
  ];
  
  // FAQ для структурированных данных
  const faqRaw = t.raw('seo.faq.faqItems');
  const faq = Array.isArray(faqRaw)
    ? (faqRaw as Array<{ q: string; a: string }>)
    : [];
  
  return (
    <div className="min-h-screen bg-gray-50 dark:bg-gray-900">
      <Header />
      <main>
        <Breadcrumbs items={breadcrumbItems} />
        <CalculatorComponent />
        <CalculatorSEO />
        <SoftwareApplicationSchema />
      </main>
    </div>
  );
}
```

---

## 🌍 Локализация (9 языков)

### Поддерживаемые языки:

1. **ru** - Русский (основной язык)
2. **en** - English (Английский)
3. **de** - Deutsch (Немецкий)
4. **es** - Español (Испанский)
5. **fr** - Français (Французский)
6. **it** - Italiano (Итальянский)
7. **pl** - Polski (Польский)
8. **tr** - Türkçe (Турецкий)
9. **pt-BR** - Português (Бразильский португальский)

### Определение локали:

**Middleware** (`src/middleware.ts`) обрабатывает определение локали в следующем порядке:

1. **Cookie** (`NEXT_LOCALE`) - сохраненное предпочтение пользователя
2. **Accept-Language header** - язык браузера
3. **Default** - русский (`ru`)

**Логика определения:**
- Проверяется cookie `NEXT_LOCALE`
- Если нет cookie, парсится заголовок `Accept-Language`
- Поддерживается маппинг региональных вариантов (например, `en-US` → `en`)
- Результат сохраняется в cookie на 1 год

### URL структура:

Все страницы имеют префикс локали:
- `https://calc1.ru/ru/construction/floor-heating` - русская версия
- `https://calc1.ru/en/construction/floor-heating` - английская версия
- `https://calc1.ru/de/construction/floor-heating` - немецкая версия

### Конфигурация i18n:

**Файл `src/i18n.ts`:**
- Определяет список поддерживаемых локалей
- Загружает переводы из монолитных файлов и категорийных файлов
- Объединяет переводы через `deepMerge`
- Обеспечивает fallback на русский язык при ошибках

**Файл `src/lib/i18n-utils.ts`:**
- Утилиты для загрузки переводов в `generateMetadata`
- Функции `loadMergedCategoryTranslations` для каждой категории
- Поддержка обратной совместимости с монолитными файлами

---

## 🔧 Принципы работы с переводами

### 1. Единообразие структуры

Все калькуляторы должны иметь одинаковую структуру переводов:

```json
{
  "calculators": {
    "calculator-name": {
      "title": "...",
      "description": "...",
      "breadcrumbs": {...},
      "form": {...},
      "results": {...},
      "seo": {...}
    }
  }
}
```

### 2. Обязательные секции

Каждый калькулятор должен иметь:
- `title` - заголовок
- `description` - описание
- `breadcrumbs.category` - категория для хлебных крошек
- `form.*` - все поля формы
- `results.*` - все поля результатов
- `seo.*` - весь SEO контент

### 3. Перевод выпадающих списков

Все значения выпадающих списков должны быть переведены:

```json
{
  "form": {
    "roomType": {
      "bathroom": "Ванная",
      "kitchen": "Кухня",
      "living": "Гостиная"
    }
  }
}
```

Использование в компоненте:
```typescript
const roomTypes = Object.entries(t.raw('form.roomType')).map(([key, value]) => ({
  value: key,
  label: value as string,
}));
```

### 4. Локализация валют и единиц

Валюты и единицы измерения локализуются в переводах:

```json
{
  "form": {
    "electricityCost": "Стоимость электроэнергии (₽/кВт·ч)",
    "area": "Площадь (м²)"
  }
}
```

### 5. Проверка переводов

**Скрипт проверки** (`scripts/check-translations.js`):
- Проверяет наличие всех ключей во всех языках
- Выявляет отсутствующие переводы
- Проверяет структуру JSON файлов

**Запуск проверки:**
```bash
npm run test:translations
```

**Скрипт синхронизации** (`scripts/sync-translations.js`):
- Синхронизирует структуру переводов между языками
- Добавляет недостающие ключи с placeholder значениями
- Конвертирует старые форматы в новые

**Запуск синхронизации:**
```bash
npm run sync:translations
```

### 6. Правила добавления новых переводов

1. **Сначала найти** существующие ключи в файле
2. **Потом добавить** только недостающие
3. **Проверить** соответствие во всех языках
4. **Не дублировать** существующие ключи
5. **Использовать kebab-case** для имен калькуляторов

### 7. Использование в компонентах

**Client Components:**
```typescript
const t = useTranslations('calculators.calculator-name');
const locale = useLocale(); // Для локализации данных (например, названий продуктов)
```

**Server Components:**
```typescript
const t = await getTranslations({
  locale,
  namespace: 'calculators.calculator-name',
});
```

**Доступ к сырым данным (для объектов):**
```typescript
const dropdownOptions = t.raw('form.dropdownName');
const faqItems = t.raw('seo.faq.faqItems');
```

---

## 📊 Структура данных калькуляторов

### Категории калькуляторов:

1. **auto** - Автомобильные калькуляторы
   - Расчет кредита на авто
   - Расчет стоимости владения
   - Расчет таможенных пошлин
   - Расчет расхода топлива
   - И т.д.

2. **construction** - Строительные калькуляторы
   - Расчет бетона
   - Расчет обоев
   - Расчет плитки
   - Расчет теплого пола
   - И т.д.

3. **finance** - Финансовые калькуляторы
   - Расчет кредита
   - Расчет ипотеки
   - Расчет депозита
   - Расчет налогов
   - И т.д.

4. **life** - Калькуляторы для жизни
   - Расчет калорий
   - Расчет расхода воды/газа/электричества
   - Расчет размера кольца
   - И т.д.

5. **math** - Математические калькуляторы
   - Базовые операции
   - Проценты
   - Площадь и объем
   - Решение уравнений
   - И т.д.

6. **time** - Калькуляторы времени
   - Расчет возраста
   - Расчет дней между датами
   - Таймер
   - Календарь
   - И т.д.

7. **health** - Калькуляторы здоровья
   - Расчет ИМТ
   - Расчет артериального давления
   - Расчет дозировки лекарств
   - И т.д.

8. **converter** - Конвертеры единиц
   - Длина, вес, объем
   - Температура, давление
   - Скорость, энергия
   - И т.д.

9. **fun** - Развлекательные калькуляторы
   - Генератор имен
   - Совместимость по именам
   - Генератор паролей
   - И т.д.

10. **it** - IT калькуляторы
    - Расчет хешрейта
    - IP калькулятор
    - Генератор QR-кодов
    - И т.д.

11. **science** - Научные калькуляторы
    - Калькулятор оценок
    - И т.д.

---

## 🧪 Тестирование

### Unit-тесты (Jest):

Тесты для логики расчетов находятся в `src/lib/calculators/__tests__/`:

```typescript
import { calculateCalculator, validateCalculatorInput } from '../calculator-name';

describe('CalculatorName', () => {
  it('should calculate correctly', () => {
    const input = { field1: 10, field2: 'value' };
    const result = calculateCalculator(input);
    expect(result.result1).toBe(20);
  });
  
  it('should validate input', () => {
    const input = { field1: -1, field2: '' };
    const validation = validateCalculatorInput(input);
    expect(validation.isValid).toBe(false);
  });
});
```

**Запуск тестов:**
```bash
npm test                    # Запуск всех тестов
npm run test:watch          # Режим наблюдения
npm run test:coverage       # С покрытием кода
```

### E2E тесты (Playwright):

Тесты пользовательских сценариев находятся в `e2e/`:

```typescript
import { test, expect } from '@playwright/test';

test('should calculate BMI', async ({ page }) => {
  await page.goto('/ru/health/bmi');
  await page.fill('[name="weight"]', '70');
  await page.fill('[name="height"]', '175');
  await page.click('button[type="submit"]');
  await expect(page.locator('.result')).toContainText('22.86');
});
```

**Запуск E2E тестов:**
```bash
npm run test:e2e            # Запуск всех E2E тестов
npm run test:e2e:ui         # С UI интерфейсом
npm run test:e2e:headed    # С открытым браузером
```

---

## 🚀 Разработка нового калькулятора

### Чеклист создания нового калькулятора:

1. **Создать логику расчетов** (`src/lib/calculators/{calculator-name}.ts`)
   - [ ] Определить интерфейсы `Input` и `Result`
   - [ ] Реализовать функцию валидации
   - [ ] Реализовать функцию расчета
   - [ ] Добавить вспомогательные функции
   - [ ] Добавить JSDoc комментарии
   - [ ] Написать unit-тесты

2. **Создать компонент калькулятора** (`src/components/calculators/{calculator-name}-calculator.tsx`)
   - [ ] Использовать `useTranslations` для переводов
   - [ ] Реализовать форму ввода
   - [ ] Реализовать отображение результатов
   - [ ] Добавить валидацию и обработку ошибок
   - [ ] Добавить комментарии к коду

3. **Создать SEO компонент** (`src/components/seo/{calculator-name}-seo.tsx`)
   - [ ] Реализовать SEO контент
   - [ ] Добавить FAQ структурированные данные
   - [ ] Добавить HowTo структурированные данные (если применимо)

4. **Создать страницу** (`src/app/[locale]/{category}/{calculator-name}/page.tsx`)
   - [ ] Реализовать `generateMetadata`
   - [ ] Реализовать основной компонент страницы
   - [ ] Добавить хлебные крошки
   - [ ] Подключить компоненты

5. **Добавить переводы** во все языки
   - [ ] Добавить в `messages/{locale}/calculators/{category}.json`
   - [ ] Добавить все обязательные секции
   - [ ] Перевести выпадающие списки
   - [ ] Локализовать валюты и единицы
   - [ ] Добавить SEO контент
   - [ ] Проверить через `npm run test:translations`

6. **Проверить работу**
   - [ ] Проверить на всех языках
   - [ ] Проверить валидацию
   - [ ] Проверить расчеты
   - [ ] Проверить SEO метаданные
   - [ ] Проверить структурированные данные

---

## 📝 Важные замечания

### 1. Именование файлов и компонентов

- **Файлы логики**: `kebab-case.ts` (например, `floor-heating.ts`)
- **Компоненты**: `PascalCase.tsx` (например, `FloorHeatingCalculator`)
- **Страницы**: `page.tsx` (стандарт Next.js)
- **Ключи переводов**: `kebab-case` (например, `floor-heating`)

### 2. Импорты

Всегда используйте алиасы путей:
- `@/components/...` вместо `../../components/...`
- `@/lib/...` вместо `../../lib/...`

### 3. Комментарии

Все функции должны быть документированы:
- JSDoc комментарии для экспортируемых функций
- Inline комментарии для сложной логики
- Комментарии на английском языке

### 4. Типизация

- Всегда используйте TypeScript interfaces
- Избегайте `any` типов
- Используйте строгую типизацию для входных данных и результатов

### 5. Обработка ошибок

- Всегда валидируйте входные данные
- Предоставляйте понятные сообщения об ошибках
- Используйте try-catch для обработки исключений

---

## 🔗 Полезные ссылки

- [Next.js Documentation](https://nextjs.org/docs)
- [next-intl Documentation](https://next-intl-docs.vercel.app/)
- [TypeScript Documentation](https://www.typescriptlang.org/docs/)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [Schema.org Structured Data](https://schema.org/)

---

**Последнее обновление**: 2024

