name: Deploy to Production

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate secrets
        run: |
          if [ -z "${{ secrets.SERVER_HOST }}" ]; then
            echo "::error::SERVER_HOST secret is not set. Please add it in repository settings."
            exit 1
          fi
          if [ -z "${{ secrets.SERVER_USER }}" ]; then
            echo "::error::SERVER_USER secret is not set. Please add it in repository settings."
            exit 1
          fi
          if [ -z "${{ secrets.SERVER_SSH_KEY }}" ]; then
            echo "::error::SERVER_SSH_KEY secret is not set. Please add it in repository settings."
            exit 1
          fi
          echo "✅ All required secrets are configured"

      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          command_timeout: 60m
          script: |
            set -e
            cd /var/www/calc1.ru || exit 1
            
            echo "🔧 Configuring Git safe directory..."
            git config --global --add safe.directory /var/www/calc1.ru || true
            
            echo "📥 Fetching latest changes..."
            git fetch origin main || exit 1
            
            echo "🔄 Resetting to match remote branch..."
            git reset --hard origin/main || exit 1
            
            echo "🧹 Cleaning untracked files..."
            git clean -fd || true
            
            echo "🛑 Stopping old containers..."
            docker compose down 2>/dev/null || docker-compose down 2>/dev/null || true
            
            echo "🔨 Building new image (this may take 20-40 minutes)..."
            echo "   Using Docker cache for faster builds..."
            # Set environment variables to speed up build
            export DOCKER_BUILDKIT=1
            export COMPOSE_DOCKER_CLI_BUILD=1
            # Use cache for faster builds, but rebuild layers if needed
            docker compose build || docker-compose build || exit 1
            
            echo "▶️ Starting containers..."
            docker compose up -d || docker-compose up -d || exit 1
            
            echo "🧹 Cleaning up..."
            docker system prune -f || true
            
            echo "✅ Deployment completed"

      - name: Verify deployment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          script: |
            echo "🔍 Checking container status..."
            docker compose ps || docker-compose ps || true
            
            echo "⏳ Waiting for application to start (90 seconds)..."
            sleep 90
            
            echo "📋 Checking container logs (last 100 lines)..."
            docker compose logs --tail=100 calc1 2>/dev/null || docker-compose logs --tail=100 calc1 2>/dev/null || true
            
            echo "🌐 Checking if application is responding..."
            MAX_ATTEMPTS=20
            for i in $(seq 1 $MAX_ATTEMPTS); do
              if curl -f -s http://localhost:3001 > /dev/null 2>&1; then
                echo "✅ Application is responding on port 3001!"
                exit 0
              fi
              echo "⏳ Attempt $i/$MAX_ATTEMPTS: Application not ready yet, waiting 5 seconds..."
              sleep 5
            done
            
            echo "❌ Application failed to start after $((MAX_ATTEMPTS * 5)) seconds"
            echo ""
            echo "📋 Final container status:"
            docker compose ps || docker-compose ps || true
            echo ""
            echo "📋 Container health:"
            docker inspect calc1-app --format='{{.State.Status}} - {{.State.Health.Status}}' 2>/dev/null || echo "Container not found"
            echo ""
            echo "📋 Last 200 lines of logs:"
            docker compose logs --tail=200 calc1 2>/dev/null || docker-compose logs --tail=200 calc1 2>/dev/null || true
            echo ""
            echo "📋 Checking if port 3001 is listening:"
            netstat -tuln | grep 3001 || ss -tuln | grep 3001 || echo "Port 3001 is not listening"
            echo ""
            echo "📋 Checking container processes:"
            docker top calc1-app 2>/dev/null || echo "Cannot check processes"
            exit 1
