# 🔐 Настройка GitHub Secrets для автоматического деплоя

## Проблема: "Error: missing server host"

Эта ошибка означает, что GitHub Actions не может найти необходимые секреты для подключения к серверу.

## ✅ Решение: Настройка GitHub Secrets

### Шаг 1: Перейдите в настройки репозитория

1. Откройте репозиторий: https://github.com/alex1c/calc1
2. Перейдите в **Settings** (Настройки)
3. В левом меню выберите **Secrets and variables** → **Actions**
4. Нажмите **New repository secret**

### Шаг 2: Добавьте необходимые секреты

Добавьте следующие секреты (каждый отдельно):

#### 1. SERVER_HOST

-   **Name:** `SERVER_HOST`
-   **Value:** IP адрес или домен вашего сервера (например: `123.45.67.89` или `calc1.ru`)

#### 2. SERVER_USER

-   **Name:** `SERVER_USER`
-   **Value:** Имя пользователя для SSH (обычно `root` или ваш пользователь, например: `ubuntu`)

#### 3. SERVER_SSH_KEY

-   **Name:** `SERVER_SSH_KEY`
-   **Value:** Приватный SSH ключ для доступа к серверу

#### 4. SERVER_PORT (опционально)

-   **Name:** `SERVER_PORT`
-   **Value:** Порт SSH (по умолчанию `22`, можно не указывать)

## 🔑 Генерация SSH ключа

Если у вас еще нет SSH ключа для GitHub Actions, выполните на сервере:

```bash
# Подключитесь к серверу
ssh user@your-server-ip

# Создайте SSH ключ специально для GitHub Actions
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions

# Добавьте публичный ключ в authorized_keys
cat ~/.ssh/github_actions.pub >> ~/.ssh/authorized_keys

# Установите правильные права
chmod 600 ~/.ssh/github_actions
chmod 644 ~/.ssh/authorized_keys

# Выведите приватный ключ для копирования в GitHub Secrets
cat ~/.ssh/github_actions
```

**Важно:** Скопируйте весь вывод команды `cat ~/.ssh/github_actions` (включая строки `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`) в секрет `SERVER_SSH_KEY`.

## 📋 Чеклист настройки

-   [ ] Создан SSH ключ на сервере
-   [ ] Публичный ключ добавлен в `~/.ssh/authorized_keys`
-   [ ] Добавлен секрет `SERVER_HOST` в GitHub
-   [ ] Добавлен секрет `SERVER_USER` в GitHub
-   [ ] Добавлен секрет `SERVER_SSH_KEY` в GitHub (приватный ключ)
-   [ ] Добавлен секрет `SERVER_PORT` (опционально, если не 22)

## 🧪 Проверка подключения

После настройки секретов, проверьте подключение вручную:

```bash
# На вашем локальном компьютере
ssh -i ~/.ssh/github_actions user@your-server-ip
```

Если подключение работает, GitHub Actions также сможет подключиться.

## 🔍 Проверка работы GitHub Actions

После настройки всех секретов:

1. Сделайте любой коммит и push в ветку `main`
2. Перейдите в **Actions** в репозитории GitHub
3. Проверьте, что workflow запустился и выполнился успешно

## 🆘 Решение проблем

### Проблема: "Permission denied (publickey)"

**Решение:**

-   Убедитесь, что публичный ключ добавлен в `~/.ssh/authorized_keys` на сервере
-   Проверьте права доступа: `chmod 600 ~/.ssh/github_actions` и `chmod 644 ~/.ssh/authorized_keys`

### Проблема: "Connection refused"

**Решение:**

-   Проверьте, что SSH сервер запущен на сервере: `sudo systemctl status ssh`
-   Проверьте firewall: `sudo ufw status`
-   Убедитесь, что порт SSH открыт

### Проблема: "Host key verification failed"

**Решение:**

-   Добавьте опцию `strict: false` в workflow (не рекомендуется для production)
-   Или добавьте fingerprint сервера в секрет `SERVER_FINGERPRINT`

## 📚 Дополнительная информация

-   [GitHub Actions Secrets Documentation](https://docs.github.com/en/actions/security-guides/encrypted-secrets)
-   [SSH Key Generation Guide](https://docs.github.com/en/authentication/connecting-to-github-with-ssh/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent)

---

**После настройки всех секретов, автоматический деплой будет работать при каждом push в ветку `main`.**
